Face recognition project using Opencv python

First, collect face data using face_data_collector
then, train data using model_train
then, detect faces using detecting_faces

Note: change the path for facedata in all three files according to your convenience.